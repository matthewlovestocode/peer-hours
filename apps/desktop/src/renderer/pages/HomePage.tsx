/** Renders the intentionally quiet default workspace while product sections are composed. */
export function HomePage() {
  return <section className="home-page"><p className="eyebrow">Peer Hours</p><h1>Make time count.</h1><p className="home-page__copy">Your community, exchanges, and network will live here. Choose a section from the navigation to explore the foundations now in place.</p></section>;
}
